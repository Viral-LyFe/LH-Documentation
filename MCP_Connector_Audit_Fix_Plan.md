# MCP Connector Security Audit — Fix & Study Plan

> Source: `LyfeHardwareMCPConnectorAuditReport.docx` (audit of `lh` app + `Viral-LyFe/mcp` fork, commit `dc3ae7f`)
> Overall score: **3.4/10 — NOT READY FOR PRODUCTION USE**
> Prepared: 2026-08-07

## Scope of the Audit

- **Audited**: two design documents, the `lh` app code, and the private MCP server fork.
- **Not audited**: the live ERPNext site (no access), and 12 of the 24 exposed record types belonging to external apps.
- **Confirmed good**: read-only guarantee holds (no write tools found), no shared service account, disabling a user blocks Claude access instantly, settings/credential screens are unreachable, financial dashboards are Super-Admin-locked, no SQL injection found, audit log can't be edited/deleted via UI, server library up to date with upstream.

The core finding: the "only shows what you could already see" half of the design is broken — masking hides a field's value in the response but doesn't stop it being filtered/sorted on, so it can be inferred anyway. That undermines most other protections.

---

## Part 1 — Study First (context before touching code)

Read/trace these before making changes, in this order:

1. `lh/lyfe_hardware/mcp_tools/doctypes.py` — the `list` tool handler, `_strip_masked_keys()`, `_masked_fieldnames`, the "excluded sub-records" note at lines 16-24 vs. the actual exposed list.
2. The MCP server fork (`Viral-LyFe/mcp`, commit `dc3ae7f`) — `run_tool()` vs `handle_call_tool()`, and where/why validation (min/max record limits) is only exercised by tests, not the live path.
3. `lh/lyfe_hardware/integrations/mcp_audit.py` — logging wrapper, the "already wrapped" marker bug, the `_require_founder_or_administrator` naming mismatch, guidance-text injection at lines 81-93.
4. `lh/lyfe_hardware/integrations/mcp_scope_guard.py` — how the connector is identified (by display name, not stable ID).
5. The four undocumented dashboard modules: `status_overview.py`, `comparison_dashboard.py`, `item_data_completeness.py`, `pm_operations_dashboard.py` — confirm actual tool count (87) vs. documented (51), and which of these bypass doctype/field permission checks entirely (they query DB directly).
6. `docs/mcp-api-reference.md` and the stale `docs/mcp_tools_api_reference.md` — reconcile which is authoritative.
7. `pyproject.toml:13` — confirm the floating dependency on the MCP server fork.
8. ERPNext OAuth Client records `4sub5lulum` and `c0hsdkmtn4` — figure out which is actually in use (requires live-site access we don't currently have).

---

## Part 2 — CRITICAL: Fix Before Any Wider Use (8 issues)

**Status as of 2026-08-07: all 8 resolved.** Issue 2 was researched and found not to be a live exploit on this Frappe version (docstring corrected instead of code changed); Issue 6 was resolved for its highest-severity doctype (`Employee`) with the remaining 19 exposed doctypes explicitly reviewed and confirmed by the business as not requiring field-level protection, including one (`Task`) with a live-reachable gap that was surfaced before the decision was made, not silently accepted. Everything else was a direct live-exploit fix, verified end-to-end and tested.

| # | Issue | Location | Fix |
|---|-------|----------|-----|
| 1 | ✅ **FIXED (2026-08-07)** — Masked fields can be inferred via filter/sort (biggest issue in the report) | fork `doctype.py:208`, `_strip_masked_keys()` in `doctypes.py` | Added `_referenced_fieldnames()` + `_require_no_masked_filter_or_sort()` to `lh/lyfe_hardware/mcp_tools/doctypes.py`, called from `_wrap_with_field_masking()` before the query runs. Rejects any `filters`/`order_by` referencing a permlevel-masked field with `frappe.PermissionError`, fail-closed, Administrator exempt (matches `_strip_masked_keys()`'s own exemption). Verified live end-to-end on `lyfe.local.local`: reproduced the original exploit (binary-searched a masked user to the exact `cost_of_goods` value `85.98` via filter-only calls, output never containing the field) — confirmed blocked post-fix at the actual registered `list_lyfe_order` MCP tool entry point. Legitimate filtering/sorting on unmasked fields and the existing `get_<doctype>` output-masking both verified unaffected. 13 new regression tests added in `lh/tests/test_mcp_field_masking.py`, all passing; existing `test_security_fixes.py` suite re-run clean (no regressions). |
| 2 | ⚠️ **RESEARCHED (2026-08-07) — report's claim does not hold on the current Frappe version; docstring corrected, no functional change needed** — "Custom BOM Items" sub-record exposed despite the file's own rule against it | `doctypes.py:42` (contradicted the note at :16-24, now corrected) | Traced `frappe.has_permission()` on a child doctype → delegates to `frappe.permissions.has_child_permission()`. Confirmed by direct testing (including simulating a role with base parent read but zero permlevel access via a monkeypatch): `has_child_permission()` (a) returns `False` outright when no `doc` is given, so `list_custom_bom_items` is unreachable by every non-Administrator user on this site — verified exhaustively, and (b) when given a real child row name, correctly resolves the row's actual parent, checks the **parent field's own permlevel**, and only then checks the parent doctype's permission — so `get_custom_bom_items(name)` is safely scoped through `Lyfe Order`'s permlevel 1 (or `Lyfe BOM`'s intentionally-open permlevel 0). No live exploit found. **Fix applied:** corrected the module docstring's factually-wrong claim ("frappe.has_permission() is a no-op for pure child doctypes... returns True unconditionally") with the traced/tested mechanism, so a future maintainer doesn't "fix" something that already works or remove a legitimate access path. `Custom BOM Items` intentionally left in the allowlist. |
| 3 | ✅ **FIXED (2026-08-07)** — 3 status-overview tools return COGS with zero permission checks | `status_overview.py:25-61`; cost columns at `lyfe_orders_status_overview.py:126,233` | Confirmed live, more severe than initially scoped: `get_orders_status_overview()`/`get_orders_reshipments()`/`get_active_reshipments()` run raw SQL with **zero** permission checks and return `cost_of_goods` (Lyfe Order permlevel 4), `reshipment_cogs`/`shipment_cost` (same real cost data), and `customer` (permlevel 2) directly — as an Engineer-role user masked from all three everywhere else, got exact values for 118 orders in one call, no inference needed (worse than Issue 1 — no binary search required). **Fix:** added `_masked_dashboard_fields()`/`_strip_sensitive_dashboard_fields()` to `status_overview.py` — strips `cost_of_goods`/`reshipment_cogs`/`shipment_cost`/`customer` from every returned row for callers lacking the matching `Lyfe Order` permlevel, checked against `Lyfe Order`'s own permlevel access as the single source of truth. Verified live: masked user now gets 0 leaked keys across all 3 tools; authorized user (`srishti@lyfehardware.com`, full permlevel access) still sees real values in all 158/158 rows — no over-masking. **Related finding fixed in the same pass (not in the original report):** `_masked_fieldnames()`/`_strip_masked_keys()` in `doctypes.py` only inspected a doctype's own top-level fields, never child tables — so `get_lyfe_order`'s embedded `order_items`/`extra_items` rows leaked a misleading `cogs: 0.0` (Frappe's core masking nulls the real value, but the key/wrong-default survives `as_dict()`, same failure mode as the already-documented top-level bug) instead of being stripped. Added `_masked_child_fieldnames()`, extended `_strip_masked_keys()` to walk child rows. Verified live: real `cogs=8.80` on a test order came back masked (key absent) for an Engineer user, and correctly visible for an authorized user. |
| 4 | ✅ **FIXED (2026-08-07)** — `cost_of_goods_sold` custom field has no protection level set at all | `patches/add_item_custom_fieldss.py:16-20` | Researched before fixing, per instruction to check current roles/permissions first: confirmed the patch file genuinely has no `permlevel`, **but** the live site already had a `Property Setter` (`Item-cost_of_goods_sold-permlevel = 1`) plus `Custom DocPerm` rows granting Factory + Super Admin read — applied directly to the DB outside version control at some point, correctly matching the documented intent. Verified against all 16 enabled users: 6 correctly blocked, 10 correctly allowed. The real gap was that this protection existed only in the DB, not in code — a fresh install/restore would silently lose it. **Fix:** new idempotent patch `lh/patches/secure_item_cogs_permlevel.py` (registered in `patches.txt`) sets `permlevel=1` on the `Custom Field` record directly and creates the Factory/Super Admin `Custom DocPerm` read grants if not already present — mirrors the existing `hide_shipping_rate_from_customer_service.py` pattern. Ran on `lyfe.local.local`, confirmed idempotent (re-run created zero duplicate rows), confirmed masking still works end-to-end via `get_item` for both a masked (Engineer) and authorized (Factory) user. |
| 5 | ✅ **FIXED (2026-08-07)** — Daily Work Log readable by every user (role "All") | Daily Work Log doctype permissions | Confirmed live: `DocPerm` for `Daily Work Log` granted `role: "All"` full read/write/create/submit — every enabled user, automatically. Checked the "correct" reference pattern (`External Learning Record`) and confirmed it uses `Employee` (`if_owner: 1`, self-only) + `HR Manager`/`HR User`/`System Manager`, no `"All"` anywhere. Verified zero records currently exist on either doctype (both are pre-launch features), so the fix carries no migration risk for existing data. Traced how `Employee` role assignment actually happens: ERPNext's `Employee.on_update()` auto-appends the `Employee` role (and creates a self-scoping `User Permission`) to the linked User on every save — confirmed live by re-saving the one real `Employee` record on this site and watching the role appear. **Fix:** replaced the `"All"` permission row in `daily_work_log.json` with `Employee` (`if_owner: 1`), and added `HR User` (matching `External Learning Record`'s `delete: 0` for that role) alongside the existing `HR Manager`/`System Manager` rows. **Follow-up (same session, user-requested):** added a `Super Admin` permission row (full read/write/create/delete/submit, not owner-restricted) matching `System Manager`'s grant level — confirmed live via the Role Permissions Manager screenshot showing all 5 final roles (`HR Manager`, `Super Admin`, `HR User`, `Employee` with "Only If Creator" checked, `System Manager`) with `"All"` no longer present. Migrated cleanly (`bench migrate`, exit 0). Verified end-to-end with a real inserted record: the owning employee can read their own log; a non-Employee/non-HR user (Engineer role) is fully blocked; an HR Manager and a Super Admin user can both read it regardless of ownership. 9 new regression tests added in `lh/lh_hrms/doctype/daily_work_log/test_daily_work_log.py` (DocPerm shape checks, live cross-role `has_permission` checks, and a full end-to-end ownership-isolation test using a real inserted `Daily Work Log` + `Employee Checkin` record) — all pass (1 test correctly skips: only 1 `Employee` record exists on this site, so the "second Employee-role user without HR override" case has no fixture to exercise against). |
| 6 | ✅ **RESOLVED (2026-08-07) — Employee fixed; remaining 19 doctypes explicitly reviewed and closed as no-action-needed per business decision** — Field-masking safeguard only configured for 1 of 12 inspectable doctypes | `doctypes.py:139-140` (`_masked_fieldnames` early exit) | Re-audited against the live site: 26 exposed doctypes (not 12 as originally estimated). Confirmed only 7 have ANY permlevel-protected field before this pass (`Lyfe Order` 52, `Quotation` 7, `Item` 1 — Issue 4, `Employee Checkin`/`Leave Application`/`Appraisal` 1 each) — **19 had zero protection.** Investigated the actual data behind the gap rather than just counting fields, on every doctype, not only the obvious ones — see full per-doctype breakdown below. Highest-severity finding: `Employee` (123 fields, 0 protected) — salary (`ctc`), bank details (`bank_ac_no`/`iban`), personal PII (`date_of_birth`, `personal_email`, `passport_number`, health insurance, exit-interview `feedback`); confirmed `HR User`/`HR Manager` had byte-identical `DocPerm` rows, no field-level distinction. Confirmed live (real inserted 2nd Employee record with fake sensitive data) that ERPNext's own `User Permission` auto-scoping already blocks a self-scoped `Employee`-role user from reading a *colleague's* record — the live risk was specifically "no HR-tier distinction," not "anyone reads anyone's salary." Second finding, explicitly flagged before closing: `Task.total_costing_amount`/`total_expense_claim`/`total_billing_amount` unmasked and reachable **today** by a real, non-elevated user (`support@chandakbrothers.net`, `Projects User` role, no System Manager/HR Manager/Super Admin) — confirmed live via the actual `get_task` MCP tool; currently latent only because no `Task` on this site has non-zero costing data yet, not because the gap doesn't exist. Raised this specific live gap back to the user before accepting "no protection needed" for it, to make sure the decision was informed rather than assumed — user confirmed Task does not need protection either. **Business decision (explicit, both rounds confirmed):** `Employee` gets fixed; all other 19 doctypes (`Lyfe BOM`, `Custom BOM Items`, `Project`, `Task`, `Project Task Status`, `SLA Rule`, `SLA Task Link`, `SLA Violation Cache`, `SLA Escalation Log`, `Sales Invoice`, `Daily Work Log`, `External Learning Record`, `Reconciliation Flag`, `Attendance`, `Employee Skill Map`, `Energy Point Log`, `Energy Point Rule`, `Job Opening`, `LMS Enrollment`) require no field-level protection — closed as reviewed, not silently skipped. **Fix (Employee only):** new patch `lh/patches/secure_employee_sensitive_fields.py` sets `permlevel=1` on 17 fields — 15 core DocFields via `frappe.make_property_setter()` (matching the existing `hide_rate_in_quotation_taxes.py` pattern) plus 2 Custom Fields (`health_insurance_provider`/`_no`) set directly — grants `HR Manager`+`Super Admin` read at that permlevel; `HR User` loses both the salary/bank group and the personal/PII group, per explicit scope confirmation. Verified live end-to-end: masked user (Employee role only) gets 0 of 17 fields via both `get_employee` and `list_employee` (even when explicitly requested in `fields`); `HR Manager`/`Super Admin` retain full visibility; Issue 1's filter-inference guard automatically extends to all 17 new fields with zero additional wiring. Idempotent (confirmed: 15 Property Setters, 2 DocPerm rows, stable across re-runs). 7 new regression tests in `lh/tests/test_employee_sensitive_fields.py`, all passing; `test_mcp_field_masking.py` (36), `test_security_fixes.py` (11), `test_daily_work_log.py` (9) all re-run clean, no regressions. |

**Issue 6 — full doctype-by-doctype disposition (26 exposed doctypes):**

| Doctype | Before | Disposition |
|---|---|---|
| Lyfe Order | 52 masked | Already protected (Issue 1/3 fixes) |
| Item | 1 masked | Already protected (Issue 4 fix) |
| Quotation | 7 masked | Already protected (shipping-rate section only; pre-existing) |
| Employee | 0 masked | **Fixed this pass** — 17 fields now `permlevel=1`, HR Manager+Super Admin only |
| Employee Checkin | 1 masked | Reviewed — no further action needed (business decision) |
| Leave Application | 1 masked | Reviewed — no further action needed (business decision) |
| Appraisal | 1 masked | Reviewed — no further action needed (business decision) |
| Lyfe BOM | 0 masked | Reviewed — no protection needed (business decision) |
| Custom BOM Items | 0 masked | Reviewed — no protection needed (business decision) |
| Project | 0 masked | Reviewed — no protection needed (business decision) |
| Task | 0 masked | Reviewed — **live gap flagged explicitly** (costing fields, real reachable user today) — no protection needed (business decision, confirmed after the gap was raised) |
| Project Task Status | 0 masked | Reviewed — no protection needed (business decision) |
| SLA Rule | 0 masked | Reviewed — no protection needed (business decision) |
| SLA Task Link | 0 masked | Reviewed — no protection needed (business decision) |
| SLA Violation Cache | 0 masked | Reviewed — no protection needed (business decision) |
| SLA Escalation Log | 0 masked | Reviewed — no protection needed (business decision) |
| Sales Invoice | 0 masked | Reviewed — no protection needed (business decision) |
| Daily Work Log | 0 masked | Reviewed — no protection needed (doctype-level access already fixed under Issue 5; business decision on field-level) |
| External Learning Record | 0 masked | Reviewed — no protection needed (business decision) |
| Reconciliation Flag | 0 masked | Reviewed — no protection needed (business decision) |
| Attendance | 0 masked | Reviewed — no protection needed (business decision) |
| Employee Skill Map | 0 masked | Reviewed — no protection needed (business decision) |
| Energy Point Log | 0 masked | Reviewed — no protection needed (business decision) |
| Energy Point Rule | 0 masked | Reviewed — no protection needed (business decision) |
| Job Opening | 0 masked | Reviewed — no protection needed (business decision) |
| LMS Enrollment | 0 masked | Reviewed — no protection needed (business decision) |
| 7 | ✅ **FIXED (2026-08-07)** — Record-count limits (min/max) exist only in tests, not the live path; 0/-1 bypasses limits entirely | fork `run_tool()` vs `handle_call_tool()`; zero-limit bug `item_data_completeness.py:158` | Confirmed both halves live before fixing: (1) `apps/frappe-mcp`'s `run_tool()` (the only caller of `jsonschema.validate()`, which checks the `limit.minimum=1` schema constraint) is called only by the fork's own tests — the live server (`server.py:345` → `handle_call_tool()` → `fn(**arguments)`) never validates the schema at all, and Frappe's own `DatabaseQuery` treats `limit=0` as "no LIMIT clause," so `list_lyfe_order(limit=0)` returned 2,168 rows instead of respecting any bound. (2) `item_data_completeness.py`'s 4 raw-SQL functions (`get_missing_items`, `get_combined_missing_items`, `get_listed_product_items`, `get_sku_bom_items`) each built `limit_clause = f"LIMIT {limit}" if limit > 0 else ""` — `limit=0`/negative dropped the clause entirely; confirmed live, `get_missing_items(limit=0)` returned all 1,982 matching rows instead of the intended 500-row default. **Fix (doctypes.py, generic list_&lt;doctype&gt; tools):** added `_clamp_list_limit()`, wired into `_wrap_with_field_masking()` for `list_` tools only — clamps `limit` into `[1, 200]` and `offset` to `≥0` before the query runs, regardless of caller input. **Fix (item_data_completeness.py, dashboard tools):** added a shared `_sanitize_limit()` helper (clamps into `[1, 2000]`, never returns an empty clause) and replaced all 4 call sites — fixed at the source so it also protects the Desk UI page, not just the MCP wrapper. Verified live end-to-end on `lyfe.local.local`: re-ran both original exploits post-fix — `list_lyfe_order(limit=0)` now returns 1 row (was 2,168), `get_missing_items(limit=0)` now returns 500 rows (was 1,982), huge limits cap correctly, normal calls unaffected. 24 new regression tests added across `lh/tests/test_mcp_field_masking.py` (extended) and `lh/tests/test_item_data_completeness_limit.py` (new), all passing; existing `test_security_fixes.py` suite re-run clean. |
| 8 | ✅ **FIXED (2026-08-07)** — MCP server dependency floats on a branch, no lock file | `pyproject.toml:13` | Confirmed live before fixing: `pyproject.toml` declared `frappe_mcp @ git+https://github.com/Viral-LyFe/mcp.git@main` — a floating branch ref. Traced exactly how this resolves: `pip show frappe_mcp` confirms it's installed **editable** at `apps/frappe-mcp`, so the actual running code is whatever commit that checkout is on, and `pyproject.toml`'s ref only matters again on the next fresh `pip install`/reinstall. Checked the currently-installed commit (`dc3ae7f`, matching the audited baseline) against `upstream/main`'s tip — currently identical, no drift today, but nothing in the dependency declaration prevented drift on a future reinstall. Also found and reconciled a stray second checkout of the same fork elsewhere on disk (`mcp/`, at an older commit `29657da`) — confirmed via `git merge-base --is-ancestor` that it's a stale-but-not-diverged ancestor of the installed `dc3ae7f`, not a conflicting version, so no action needed there beyond noting it. **Fix (pin):** `pyproject.toml` now pins the exact full commit hash (`dc3ae7f947918bbbc4a34a8dc5551899d3fbc71d`) instead of `@main`, with an inline comment documenting that the pin and the `apps/frappe-mcp` checkout must be bumped together (the editable install means changing the pin alone does nothing until the checkout is updated to match). **Fix (automated write-tool backstop):** new `lh/tests/test_mcp_dependency_pin.py` — inspects the **actual 98 registered MCP tools** at runtime (not just the fork's source), checking (a) every tool name starts with `get_`/`list_`, (b) no tool's unwrapped source calls a write-capable method (`.save(`, `.insert(`, `.submit(`, `frappe.db.set_value(`, etc.) — **fails closed** if a tool's source can't even be inspected, rather than silently skipping it, (c) no tool runs a SQL statement containing an `INSERT`/`UPDATE`/`DELETE`/`DROP`/`ALTER`/`TRUNCATE` verb, and (d) the vendored fork's own `build_tool()` still raises on `create`/`update`/`delete` operations. Verified the checks actually catch what they claim to: injected three separate simulated rogue tools (a plainly-named write tool, a `get_`-named tool that secretly calls `.save()`, and a `get_`-named tool that runs a raw `UPDATE` via `db.sql()`) and confirmed each is caught by a different one of the three detection layers; also confirmed the fail-closed behavior fires correctly for an uninspectable (no backing source file) tool rather than silently passing it — an early version of this test had exactly that blind spot, caught and fixed in the same pass before shipping. No lock file added — `uv`/`poetry` lock files aren't part of this project's toolchain (confirmed no existing `poetry.lock`/`uv.lock`); the commit pin plus the automated test are the equivalent guarantee for this stack. 5 new tests, all passing; `test_mcp_field_masking.py` (36), `test_employee_sensitive_fields.py` (7), `test_security_fixes.py` (11), `test_daily_work_log.py` (9), `test_item_data_completeness_limit.py` (11) all re-run clean. |

---

## Part 3 — HIGH PRIORITY (Issues 9–20)

| # | Issue | Fix |
|---|-------|-----|
| 9 | 36 of 87 tools (dashboards) never went through security design/docs | Document all 87 tools; review each dashboard tool's return data + caller roles; centralize role lists into one source instead of hand-copied lists. |
| 10 | Sensitive fields duplicated under different, unprotected field names (e.g. `cj_consignee_email` vs protected customer email; Shopify totals vs protected order value; `ship_to_city` vs protected location) | Raise protection level on every unprotected twin to match its protected counterpart; sweep all exposed doctypes for the same pattern. |
| 11 | Audit log stores full answers (up to 2000 chars incl. Super-Admin-only data); any System Manager can read/export it all | Restrict log read access to Super Admin; give others a redacted/summary view only. |
| 12 | Failed log writes are swallowed silently — request still succeeds unlogged | If the log write fails, fail the request (don't answer unrecorded). |
| 13 | Double-wrap bug causes 39 tools to log twice + run permission checks twice | Set the "already wrapped" marker correctly in `mcp_audit.py`/`doctypes.py:195`. |
| 14 | Free-text "guidance" field (edited by any System Manager, no length cap) is injected into every Claude response as if it were instructions | Restrict edit to Super Admin; cap length; pass as quoted reference data, not as instructions. |
| 15 | Token scope guard matches connector by *display name* — renaming it silently disables the guard | `mcp_scope_guard.py:52-60` — identify by permanent record ID or dedicated flag; fail closed (block) on lookup failure, not open. |
| 16 | Stale doc claims HR data is not exposed (it is) | Delete `docs/mcp_tools_api_reference.md` or replace with a pointer to the current doc. |
| 17 | Raw DB error messages returned to caller (leaks schema) | Return generic error to user; keep detail in internal logs only. |
| 18 | Staff performance rankings (by name) reachable by CS/Factory roles with no HR access | Restrict `get_pm_user_efficiency` to management roles, or aggregate to team level (strip names). |
| 19 | Two live OAuth Client records for the same connector (65 + 11 tokens), ownership unconfirmed | Confirm which is active; disable (don't delete) the unused one; revoke stale tokens. **Requires live-site access.** |
| 20 | Zero automated tests cover the connector despite 110+ test files in the app | Add tests for: field masking, sub-record exclusion, settings-screen block, token scope guard, disabled-account block, financial role gating, record-count limits. |

---

## Part 4 — MEDIUM / LOW PRIORITY (Issues 21–28)

| # | Issue | Fix |
|---|-------|-----|
| 21 | Audit log editable from server CLI (docs overclaim "nobody can edit it") | Correct documentation wording; restrict CLI-level access where feasible. |
| 22 | Logged answers truncated at 2000 chars (loses the useful tail); no row-count/requested-fields captured; connector-identity field never filled | Capture row count + requested fields before truncating; populate connector identity. |
| 23 | Audit log has no index, unbounded growth; one dashboard does up to 2000 per-row DB lookups | Add indexes; switch numbering scheme; batch the repeated lookups; define an archiving policy. |
| 24 | Role Visibility Guide has 2 factual errors (Engineer row, Super Admin row) | Correct both table entries. |
| 25 | `_require_founder_or_administrator` no longer checks what its name implies; 4 near-duplicate role checks; connector name hardcoded | Rename function; consolidate duplicated role-check logic; move connector name into settings. |
| 26 | Docs claim write-tools were "removed" from the server lib — they were never there | Correct the historical claim in docs. |
| 27 | Two independent field-masking layers, neither aware of the other; some dashboard docs mention fields the queries don't actually return | Document which layer is authoritative; correct mismatched descriptions. |
| 28 | Unclear whether fixes were verified in prod or only on test site (doc contradicts itself) | State the production verification status plainly and accurately. |

---

## Part 5 — Suggested Execution Order

1. **Critical fixes 1–8** — do not expand connector access to more users/roles until these are closed. Issue 1 (filter/sort inference) is the single highest-value fix — it undermines everything else while open.
2. **High-priority fixes 9, 13, 15** — these are cheap, mechanical, and remove entire classes of silent failure (undocumented tools, double logging, name-based guard bypass).
3. **High-priority fixes 10, 11, 14, 18** — data-exposure fixes affecting confidentiality of specific fields/roles.
4. **Issue 19** — needs live-site access; flag for whoever holds ERPNext admin credentials.
5. **Issue 20** — add regression tests for every fix above as it lands, so it can't silently regress.
6. **Medium/low items 21–28** — documentation accuracy and hygiene; batch these once critical/high items are done.

## Open Dependencies / Blockers

- No access to the live ERPNext site — Issue 19 (duplicate OAuth clients) and live verification of Issue 4's field-level permission (`cost_of_goods_sold`) cannot be completed from code alone.
- 12 of 24 exposed record types belong to external apps outside this audit's code access — these still need a pass once accessible.
